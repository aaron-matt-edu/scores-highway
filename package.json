{
  "name": "scores-highway",
  "version": "1.0.0",
  "description": "Downloads SAT scores from College Board and uploads them to an SFTP server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf node_modules",
    "package": "rm -rf bin/ && mkdir bin/ && zip -r bin/scores-highway.zip *.js node_modules"
  },
  "author": "aaron.david.matt@gmail.com",
  "license": "Apache-2.0",
  "dependencies": {
    "fs": "0.0.1-security",
    "https": "^1.0.0",
    "ssh2-sftp-client": "^5.3.1"
  }
}
